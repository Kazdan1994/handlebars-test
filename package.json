{
  "name": "variables-in-browser",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/index.ts --bundle --minify --outfile=dist/index.js --servedir=.",
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/index.js"
  },
  "devDependencies": {
    "@types/mustache": "^4.2.2",
    "esbuild": "^0.17.8",
    "prettier": "^2.8.4",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "mustache": "^4.2.0"
  }
}
